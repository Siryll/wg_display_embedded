# Common

Shared data models used by both the embedded app and the web frontend.
Build for `no_std` by default, includes the `yew` feature used when built by the frontend to include frontend only features.
